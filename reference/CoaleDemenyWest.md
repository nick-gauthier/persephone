# Coale-Demeny West Female Model Life Table Siler Parameters

Siler function parameters fit to Coale & Demeny West model life tables
for females. Each object is a one-row data frame with columns: a1, b1,
a2, a3, b3, name.

## Usage

``` r
CoaleDemenyWestF3

CoaleDemenyWestF5

CoaleDemenyWestF11

CoaleDemenyWestF15

CoaleDemenyWestF17

CoaleDemenyWestF21
```

## Format

A data frame with 1 row and 6 columns.

An object of class `data.frame` with 1 rows and 6 columns.

An object of class `data.frame` with 1 rows and 6 columns.

An object of class `data.frame` with 1 rows and 6 columns.

An object of class `data.frame` with 1 rows and 6 columns.

An object of class `data.frame` with 1 rows and 6 columns.

An object of class `data.frame` with 1 rows and 6 columns.
