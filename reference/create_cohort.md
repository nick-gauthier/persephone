# Create the initial cohort data frame

Create the initial cohort data frame

## Usage

``` r
create_cohort(cohort_size)
```

## Arguments

- cohort_size:

  Number of agents in the starting cohort

## Value

A data frame with columns: agent_id, age, lesion, dead, in_sample
