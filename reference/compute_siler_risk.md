# Compute Siler hazard for a given age

Compute Siler hazard for a given age

## Usage

``` r
compute_siler_risk(k, mortality_regime)
```

## Arguments

- k:

  Age (integer timestep)

- mortality_regime:

  Data frame with Siler parameters (a1, b1, a2, a3, b3)

## Value

Numeric hazard value
